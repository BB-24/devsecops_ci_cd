# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: tic-tac-toe-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: tic-tac-toe
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007